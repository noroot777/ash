# CLAUDE.md

## 工作约定

- 随时留意需求是否已经变动到「现有代码结构不再合适」的程度。一旦判断需求变动足够大、值得重构，就大胆重构，不要为了迁就旧结构而打补丁。
- 前端禁用浏览器原生弹窗：确认对话框用 `ConfirmModal`、其它弹层用 `Modal`（均在 `web/src/Modal.tsx`），报错/提示用 `toast`（`web/src/toast.tsx`）。不要用 `window.confirm / prompt / alert`——它们样式不一致、阻塞且无法做成应用风格。
- 做出关键性决定或确立新约定时（技术选型、目录/数据结构、跨模块的统一规则、刻意的取舍等），自行判断是否值得写进本文件；值得就主动补一条。理由：本文件在仓库根目录，会被自己、后来的人、以及在本仓库 cwd 下运行的 CLI 派生 agent 自动读到——写进来约定才真正生效，只存在于代码习惯里就会被漏掉。
- 任务完成走严格协议：**exit 0 ≠ done**。agent 必须在回合内调 harness MCP 的 `complete_task`（POST `/tasks/:id/complete`）确认目标达成，结算才落 done；未确认的正常退出记 failed（时间线附一行说明）。结算规则单点在 `settleTaskStatus`（`server/src/orchestrator.ts`），优先级：**续聊回合（`followUpFrom`）** > 手停 canceled > 组暂停 paused > 提问 paused（question）> 检查点 paused（resumePrompt）> exit≠0 failed > 已确认 done > 未确认 failed。协议通过 prompt 前言（fresh run）/ 消息尾部提醒（reply/resume 回合）注入，taskId 一并告知。逃生口：`HARNESS_LAX_DONE=1` 退回「exit 0 即 done」（接没配 harness MCP 的 agent 时用）。理由：CLI 正常退出不代表目标达成（agent 报错后退出照样 exit 0），假 done 会误推进队列、错误唤醒下游任务。
- 完成确认**同时落库**：`/tasks/:id/complete` 写 `tasks.complete_confirmed_at` 并置内存标记（`confirmDone`），`settleTaskStatus` 两边任一命中即算确认、无条件清库。理由：确认走 HTTP 打到**监听**进程，而跑这个回合的未必是同一个进程（2026-07-27 事故：一个没监听但调度器还活着的僵尸实例在跑任务，agent 的 complete_task 落进了监听进程的内存 Set，结算那边什么都没看见 → 明明确认了却记 failed，队列跟着停摆）。同一事故的另一半兜底在 `server/src/index.ts`：**listen 失败（EADDRINUSE）必须 `process.exit(1)`**，绝不能被 `uncaughtException` 兜底吞掉后留成「不监听但还在派任务」的僵尸调度器；单实例锁（`acquireDbSingletonLock`）是第一道闸，它是第二道。
- `tasks.stage` 是与 `TaskStatus` **正交**的展示/协作字段，绝不进入 `selectNextInQueue`、`queueBlockers`、`settleTaskStatus` 等调度结算语义；枚举顺序统一来自 shared 的 `STAGE_ORDER`：`implemented / verifying / verified / verify_failed / awaiting_acceptance / merged / accepted`。agent 通过 `report_stage`（POST `/tasks/:id/stage`）上报，团队调度台 `mode:"team"` 刻意拒绝、具体执行者与普通任务在未归档时均可报；更新走独立 SSE `task.stage`，并向最近任务会话 transcript 写系统消息。协议注入单点在 `server/src/orchestrator.ts`：fresh run 的 `COMPLETION_PROTOCOL` 拼 `STAGE_PROTOCOL`，reply/resume（含 follow-up）的尾部提醒拼 `STAGE_REMINDER`；共享执行者（`parentId` 指向 team 且 `useWorktree=false`）按任务字段走短协议，验证后停在 `verified/verify_failed`，不引导 `awaiting_acceptance/merged/accepted` 或单独 `accept_task`。其中「验证」必须是真实运行验证，web 要起服务并用浏览器/截图确认，不能把读代码或只过编译当验证。
- **「验收通过」是确定性后端动作**（POST `/tasks/:id/accept` / MCP `accept_task`），只在用户明确表示最终验收通过时触发，且永远不改 `TaskStatus`：running/queued 拒绝，其余停着的状态均可验收。独立 worktree 任务以 `harness/<id8>` 为源、`worktreeBase`（空则项目当前 symbolic branch）为目标，先判 ancestor，再试不动工作区的 `git fetch . <src>:<target>` fast-forward；非 FF 用临时 worktree `git merge --no-ff`，冲突只列文件并 abort，绝不强合。目标恰在项目目录检出时，只有 `git status --porcelain` 为空才允许原地 merge；脏则报告不操作。确认已合并后先 prune，再删任务 worktree，最后只用 `git branch -d` 删分支（绝不用 `-D`，删不掉就停在 merged 如实报告）。三分类：普通任务保持原有验收语义；显式 `useWorktree=true` 的独立 worktree 执行者保留完整验收、合并与清理能力；`parentId` 指向 team 且 `useWorktree=false` 的共享执行者完全退出人工验收，单独 accept 一律 409（提示改为验收团队整体），团队级 accept 成功后统一合并/清理共享 worktree，并逐个把尚未 accepted 的共享执行者 stage 联动为 accepted。未启用 harness worktree 的普通任务/团队只标 accepted，因为不存在 harness 分支可清理。本规则是旧约定「不自动删 worktree/分支」的唯一显式例外。
- 续聊（follow-up）：任务已经到终态（done/failed/canceled）后用户又发消息，这一轮是**任务之后的对话**，不是任务的执行——`continueTask` 把续聊前的终态记进 `tasks.follow_up_from`（只认真人消息：带 `opts.system` 的 retry/运行/队列推进/唤醒照旧算执行），结算后清空。两条推论：①**队列一律按 `followUpFrom` 看待该成员**（`queueStatus()`，`server/src/scheduler.ts`，`selectNextInQueue` 与 `queueBlockers` 共用）——既不算「有人在跑」冻住整条线，也不会被当可启动项拉起；②**续聊只能让任务变成 done（本回合亲口确认），不会让它变差**——没确认/异常退出/手停/组暂停一律回到原终态，回合里提问也只记 `question` 不改状态。回合尾巴换成 `FOLLOW_UP_REMINDER`（别拿「不确认就 failed」吓唬 agent）。理由：给已完成任务发一句「再发布一下」，既不该冻住后面的流水线，也不该把 done 打成 failed（2026-07-27 事故）。
- `reconcileInterrupted`（重启后收拾 running/queued 残留）**逐个走 `setTaskStatus` 单点**，不用批量 UPDATE：单点才会维护 startedAt/endedAt、广播 `task.status`、并触发 `advanceQueueFromTask`——否则重启把队列 head 打成 failed 后没人推进，整条串行队列一直停着等人手点。被打断的续聊回合回到 `followUpFrom` 那个终态（别把早就完成的任务记成 failed）。
- 队列失败不拖链：head 为 `failed` 时**透明跳过**（同 done/canceled），failed 结算也触发 `advanceQueueFromTask` 推进——失败的任务留在原地等用户重试/处理，后面的照常跑；要「挡住下游等人拍板」用 `awaiting_review`（唯一仍链停的状态）。理由：串行流水线一个环节挂了不该冻结整条线（用户可随时单独重试失败项）。「透明跳过」是同一条规则的两个口径，两处必须一致：自动推进在 `selectNextInQueue`（`server/src/scheduler.ts`），手动点运行在 `queueBlockers`（`server/src/queues.ts`，`/run` 与 `/fire` 共用）——done/canceled/failed 三种都不算挡路。
- **一条 queue 同一时刻至多一个成员在跑**，守卫单点在 `selectNextInQueue`（`server/src/scheduler.ts`）：整体扫一遍，只要有成员 running/queued 就一个都不启动，**不能只靠「顺序扫到才发现」**。理由：被透明跳过的终态任务（典型：failed）一旦回到 backlog，它的位置就排在正在跑的那个**前面**，顺序扫描先命中它就并跑了（实测：05 失败 → 06 起跑 → 05 重新排队 → 两个一起跑）。守卫放在这一层，`advanceQueue` / `runGroup` / queues 的 reorder·remove·insert / requeue 所有入口一起受益。
- 「重新排队」的唯一入口是 `POST /tasks/:id/requeue`（UI 按钮、MCP `requeue_task` 都走它）：一次原子做完「failed/canceled → backlog + 定位置 + 推进队列」。**位置规则**：队列里排在它后面的成员有人已经开跑过（`startedAt` 非空或此刻 running/queued）→ 移到**队尾**（原位置已名存实亡）；没人跑过 → 原位不动（整组还没启动时尊重用户编排的顺序）。判定与重排是纯函数 `isOvertaken` / `tailOrder`（`server/src/queues.ts`，单测在 `server/scripts/test-queue-order.ts`）。理由：前端曾用「PATCH backlog + runGroup」两步拼，中间那一瞬间就是并跑窗口。
- 分组暂停结算 **paused**（不是 canceled）：POST `/groups/:id/pause` 对 running 成员调 `stopTask(id, "paused")`，run loop 结算落 paused；canceled 仅来自用户手停单个任务。理由：canceled 被队列透明跳过，恢复分组会错启下一个；paused 占住 head，恢复（`/groups/:id/run`）时从原 CLI 会话续跑被打断的那个。「组暂停打断」的识别特征是 paused 且无 resumePrompt/question（这是该组合的唯一来源）——parallel 组（无 queue）恢复时 `runParallel` 靠它把被打断任务一并唤醒；debate 任务无 resume 机制，被组暂停杀掉仍落 canceled。
- 取消/打断任务的唯一正道是 `stop_task`（POST `/tasks/:id/stop`）：三层击杀（`killChild`，claude/codex 共用）——①进程组信号（spawn 时 detached 成组，kill -pid）；②继承 fd 追踪（spawn 时把本次运行专属追踪文件的 fd 作为 stdio[3] 传入，stop 时 `lsof -t` 反查持有者 + 从持有者向下走 ppid 树补漏）逮住 nohup/自开进程组逃出去的孤儿（真实案例：暂停分组后 TTS 流水线五口之家继续后台跑）；③2s 后统一补 SIGKILL。由 run loop 结算 canceled；queued 没进程的直接落 canceled。**对 running/queued 任务 PATCH status 一律 409 拒绝**——只改数据库不停进程会三连错：canceled 提前推进队列（串行变并行）、活着的 agent 调 complete_task 吃 409、结算把 canceled 覆盖成 failed（2026-07-21 事故，见 codex 线程 019f8d03）。失败告警文案不指控「agent 未调用」，因为确认也可能是被 409 拒掉的。
- **三层击杀只覆盖 harness 自己 spawn 的进程树**，`killChild` 之外还有一类杀不到的东西：**旁路会话**——agent 通过 IPC 请求一个独立常驻应用代跑的活。实例：codex 的 computer use 由 `/Applications/ChatGPT.app` 侧拉起 `SkyComputerUseService`（**开机级全局单例**，跟着宿主 App 一起启动，不是每任务一个），harness 的进程组/fd/ppid 三条线索一条都够不着它。处理规矩：①**已知有效的唯一手段是杀掉 agent 进程本身**——CUA 服务有 `shouldTerminateWhenNoClientsRemain` 语义，codex 客户端一死它就自行退出（2026-07-27 实测：`stopTask` 三层击杀掉执行者后，`SkyComputerUseService` 随即消失，屏幕指示器跟着走）；②**`SkyComputerUseClient turn-ended` 这条路已被实测证伪，别再试**——在**活跃** CUA 会话上手动调过 4 种 payload（`thread_id` 用真实 `cliSessionId`、加空 `turn_id`、`codex.app_session_id`、`threadID`），**全部 `exit 0` 且毫无效果**，服务照活、执行者照常截图；那个 CLI 对任何输入（包括非 JSON）都静默返回 0，「成功」是假象；③**兜底绝不自动杀 CUA 服务**——检测到残留只如实上报（`GET /tasks/:id/team/cua-status`），强杀走用户主动点的显式端点（`POST /tasks/:id/team/kill-cua`）并明示副作用。仍未验证的一条：agent **正常完成**（而非被杀）时服务会不会残留——两次对照实验都因 codex 选择用 Orca 截屏而没能触发 CUA，**别把「没验到」说成「不会发生」**。理由：那是 harness 管辖范围之外的全局单例，杀它会外溢到用户在 ChatGPT 里的其它会话；这类不可逆又外溢的操作，默认必须是「告诉用户 + 给一个他自己按的开关」。另注意 `pauseGroup` 只处理 `running`/`queued` 成员，**已 `done` 执行者遗留的旁路会话是清理盲区**，所以 `haltTeam` 的扇出必须遍历全部执行者 session 而不限状态（2026-07-27 事故：用户点「停止全组」时执行者早已全部 done，鼠标却还在屏幕上动）。
- **同一个数据库文件同一时刻只允许一个 server**，守卫在 `server/src/singleton.ts`（`index.ts` 先拿锁、再动态 import 会打开 DB 的模块）。**锁的粒度是 DB 文件路径，不是端口也不是仓库目录**——`PORT=x HARNESS_DB=/tmp/y.db` 起 throwaway 实例验证是既定工作流，必须继续允许并存。锁文件写 PID + 进程启动时间 + 端口 + token：判存活用 `process.kill(pid,0)`，再比对 `ps` 的进程启动时间**防 PID 复用误判**；释放时校验 pid+token 才 unlink，免得删掉别人的锁。逃生口 `HARNESS_ALLOW_MULTI=1`。理由：第二个实例**不占端口所以 `lsof` 查不出来**，但它的 `startScheduler` 照样 30s tick 同一个 DB、照样拉起 agent，而它起的子进程只在**它自己内存的 runs map** 里——UI 的停止请求打到另一个实例，`stopTask` 查不到就只改数据库不杀进程，于是「点了停止但进程还在跑」（2026-07-27 实测：一个 7/22 起的幽灵实例活了 5 天没人发现）。
- **schema 里删掉字段或整表时，同一轮把老库对象也退役掉**：列登记进 `RETIRED_COLUMNS`，表登记进 `RETIRED_TABLES`（`server/src/db/index.ts`）；`ensureSchema()` 末尾分别按 `PRAGMA table_info` / `sqlite_master` 判存在再 drop（幂等；失败只 warn 不拦启动），同时删掉对应的建表和 `ALTER ... ADD COLUMN`（否则每次启动一加一删打架）。登记前提：全仓 grep 确认没有任何读写，且数据已无恢复价值。理由：留着不删的代价不是那点空间，是**每次 `db:push` 都要拿「about to delete X with N items / THIS ACTION WILL CAUSE DATA LOSS」吓唬人一次**，真正该看的 schema 变更淹没在里面，久了就养成无脑 abort 的习惯。
- **新建任务是内嵌面板不是弹层**：点新建后右侧详情区整个换成 `TaskComposer`（`web/src/TaskComposer.tsx` + `web/src/composer/*`），开关状态单点在 `useComposer`（`web/src/useComposer.ts`）。因此它**不进 `anyModal`**——j/k/c 这些全局键在它开着时照常生效。任何新增的「打开新建」入口都必须走 `openComposerAt`：记下当前选中塞进 `returnTo`、再把选中清空（列表不高亮任何一行），取消时切回去；反过来，任何会切走选中的动作（点列表、j/k、`openTask`）都要先 `closeComposer()`，否则右侧还挂着单子、左侧却高亮了别的任务。已经开着时再调 `openComposer` 只换模式、**绝不冲掉用户填的内容**，除非带来一份新的种子正文（随手记合并建任务），那才 `seq+1` 重新播种。Esc 仍复用 `useEscape` 的栈，所以 preset 对话框、新建分组等真弹层照旧优先关闭。理由：这张单子是主工作区的一个状态，不是打断你的对话框——用弹窗时正文被挤在 640px 里、还得靠「展开/收起」找地方写，而它恰恰是整张单子里最该大的那一块。
- **随手记按创建时的当前项目归属**（`notes.project_id`），正文原样保存、不做 AI 解析也没有独立标题；合并创建任务后不删除，只用 `task_id` 回链所建任务。命令面板的序列快捷键统一声明在 `Command.keys`、匹配单点在 `web/src/CommandPalette.tsx`，不要为 NI/NL 另写特判。
- **worktree 没了要先想「能不能恢复」，恢复不了才重建，而且重建必须打断 agent 的记忆**（`prepareWorktree`，`server/src/git.ts`）。三档：目录还在 → 复用；目录没了但分支还在 → `git worktree add <path> <branch>` **恢复**（文件和提交原样回来，这是最该走的一档）；目录和分支都没了 → `-b` 建空壳并返回 `fresh:true`。目录被手删会留下陈旧注册（git 仍占着那个分支），所以建之前先 `git worktree prune`，把「我直接删了文件夹」拉回可恢复。`fresh` 必须一路传到调用方：**CLI 会话存在工作目录之外**（`~/.claude/projects/<转义 cwd>/`），`--resume` 接回来的 agent 记忆完好、文件却没了，不明说它就会接着「把最后一点补完」，产出建立在幻觉上——比直接报错更坏，因为要到 review 才发现。单任务走 `WORKSPACE_RESET`（同时写进时间线，刷新后可见）、团队调度台走 `LEAD_WORKSPACE_RESET`。回归测试 `npm -w server run test:worktree-recovery`。
- **预检失败的 `'error'` 事件不能立刻 emit —— 必须等到有人监听**（`failedChild`，`server/src/executors/spawn.ts`）。执行器返回的 `events` 是**惰性 async generator**（`parseClaudeStream` / `parseCodexStream`），它内部那句 `child.on("error")` 要等 run loop 第一次迭代才挂上，中间隔着好几个 await。抢在那之前 emit = 没有监听者的 `'error'`，EventEmitter 直接升级成 uncaughtException，被运行期兜底一吞，run loop 就永远等不到 `done`：**任务卡死在 running、`stopTask` 返回 true 却停不掉、再发消息被 409 挡回，只有重启才能解**（2026-07-28 实测：在一个 worktree 已被删除的任务里回复即可复现）。用 `newListener` 钩子把错误压到有订阅者时再发。推论：以后凡是「同步造一个假 child 立刻报错」的写法，都要先问一句「消费者这时候订阅了吗」。
- **启动期的致命错误必须 `exit(1)`，绝不能被运行期的兜底处理器吞掉**（`server/src/index.ts`）。两条硬规矩：①**`startScheduler()` 只在 listen 成功后的回调里调用**——一个连 HTTP 回调都收不到的进程，绝不许去轮询 schedules、拉起 agent；②顶部那两个 `unhandledRejection`/`uncaughtException` 兜底按 `startupComplete` 分流：启动期一律 `exitAfterStartupFailure`（关掉已绑定的 server、`singletonLock.release()`、exit 1），运行期才是原来那句「别让野生 SSE 异常搞垮 server」的只记录。释放锁走 `release()` 的 pid+token 校验，**不能误删同 DB 那个正常在跑的实例的锁**。理由：单实例守卫只挡「同一个 DB」，**端口撞车是另一条产幽灵的路**——`PORT=4317 HARNESS_DB=/tmp/x.db` 能顺利拿到 DB 锁，然后在 listen 那步撞死；旧代码里 scheduler 已经先起来了，EADDRINUSE 又被通用兜底接住只打日志不退出，于是留下「没有端口、但调度器在轮询生产 DB」的活死人（2026-07-27 实测复现：修复前第二个实例在 EADDRINUSE 后照样存活并 `scheduler started`；修复后 exit 1、日志里 `scheduler started` 出现 0 次）。而「不同 DB 并存」是既定的 throwaway 验证工作流，不能靠收紧锁粒度来解决。
- **停止/暂停类操作必须在 UI 上留下持久可见的状态**，只弹一个 toast 不算数。判据：用户刷新页面后仍能看出「我停过」。反例（2026-07-27）：点「停止全组」后端全做对了——进程 SIGTERM、内部组置 `paused`、会话写入系统提示——但按钮原样、状态徽标不动、`paused` 前端压根没渲染，用户只能得出「按钮坏了」。更坏的是它**掩盖了真问题**：界面若明说「已停止，0 个执行者被打断（都已完成）」，用户立刻就知道还在动的鼠标不归 harness 管，而不是把一个架构缺口误报成 UI bug。
- `/team` 团队模式：一个 `mode:"team"` 任务就是常驻调度台，状态只有忙（`running`）/闲（`idle`），**归档才是结束**；调度者不参与严格完成协议、不得调 `complete_task`，手头一轮说完只回到待命。执行者则是真实的普通任务：`tasks.parentId=<调度台 taskId>`，并落在 `groups.owner_task_id=<调度台 taskId>` 的内部组里，继续走严格完成协议、队列、单独重跑/换执行器/diff 等既有机制。执行者只在三种情况唤醒调度者：提问、失败、以及 `done && report_back=1`；普通完成静默，不再走 `scheduledMessages`。用户插话统一走 `POST /tasks/:id/reply`：在线时 `interrupt + send` 即时进入同一常驻会话，空闲回收后用同一个 `cliSessionId --resume` 接回，时间线实时与落盘一致。团队的 worktree **默认仍关闭**，但创建团队时可显式开启；开启后调度台与默认执行者共享同一个 worktree，某个执行者再显式 `useWorktree:true` 才在共享分支之上另开隔离。理由：整队必须看见同一份文件，同时执行者仍保留可调度、可追踪、可恢复及按需隔离的真任务语义。
- 普通任务（`mode:"single"`）回复框里的 `/team` / `/pair` 是**派生命令**：大小写不敏感且按词边界识别，**命中即在回复框上方弹出配置卡**（live 预览：不抢焦点，命令后的尾巴文本实时同步进附言/辩题；回车定稿——输入框清空、焦点移进卡片，此后输入不再影响卡片，用卡上的 X 关闭），命令文本绝不进入当前 agent 会话；新任务统一写 `originTaskId=<来源 taskId>`，正文带来源标题/目标/状态、可用时的最新完整会话路径和用户补充。配置卡字段与新建面板同款（`TeamExecutorFields` / `DebateComposerFields`，含任务级模型/思考强度覆盖），不另造第二套选择器。派生任务的 worktree **不显式传 `useWorktree`**，跟随 `createTasks` 单点的全局 `worktreeDefault`；来源任务的 `harness/<id8>` 分支仍存在时以它为 base（默认关闭时 base 由服务端清空，卡片提示会明说不含来源分支改动），确保来源任务的代码改动进入团队执行或辩论上下文。
- `team_presets` 是全局的团队创建快捷方式，只保存可配置的 `TeamConfig` 组合并在新建团队时一次性填入；套用后 task/team 与预设**没有持续绑定**，后续微调、重命名或删除预设都不回写已创建团队。预设里的执行器 id 悬空时，读取展示与套用都按对应 lead/worker 类型降级到当前默认执行器。
- **凡是对 `tasks.status` 做校验的地方，都要先问一句「这条对常驻调度台适用吗」**。一次性任务的状态机是「跑→终态」，`status !== "running"` 约等于「没在干活」；常驻调度台的 status 只在 `beginTurn`/`endTurn`（`server/src/team/session.ts`）之间来回切，重启后接回时更是 `idle`，**它明明活着却会被这类挡板拦住**。已知需要 team 例外的：`/ask`（调度者向用户提问）、`/answer`、`/reply`、`/run`（idle 不是终态，点运行 = 接回同一 CLI 会话）；明确不适用的：`/complete`、`/pause`（调度者不走严格完成协议，也没有检查点语义）。理由：2026-07-27 事故——`/answer` 想到了调度台、`/ask` 没有，于是调度者带 4 个候选选项提问被 409 拒，**网页上那张可点的候选卡片压根没机会渲染**，用户看到的现象是「这个功能你没做吧」。同一个文件里两个对称端点只改了一个，这类漏点靠通读发现不了，只能靠「改一处就 grep 一遍所有 status 校验点」。
- **团队的「结束」只有归档一种**，UI 上的「收工」是另一个正交概念：`isTeamSettled`（`shared/src/team.ts` 单点，web/mobile 共用）= 调度台不忙 **且** 没有执行者处于 running/queued/paused。收工只影响呈现——时间轴右端收到最后一次活动（不再空走到「现在」，右侧标签改成实际收工时刻）、`useTick` 停掉按秒重算、「停止全组」隐藏（没东西可停）。**收工 ≠ 被停止**：被 `haltTeam` 停过的团队（内部组 `paused`）即使也收工了，仍然要显示「恢复全组」；自然干完活的团队没什么可恢复的。两个判据必须各算各的，别用一个布尔糊过去。理由：把「没归档」当成「还在进行中」，会让时间轴一路空走到现在——有内容的色条被挤成一小坨，「谁跟谁在并行」这个时间轴唯一要回答的问题反而看不出来了。
- `ask_question` 的候选答案（`tasks.question_options`，json `string[]`）是**建议答案不是单选题**：网页把每条渲染成按钮，**点一下是把该选项原文填进输入框、不发送**——已有手写内容时换行追加，确认后再点「发送答复」才走 `POST /tasks/:id/answer`。这一步是刻意的：这类答复往往是不可逆决策（选「先做 A」会立刻派出一批执行者），点错一次代价不小；填入而非直发也才对得上「输入框始终留着供自由作答（答别的、答组合都行）」。答复链路、resume 语义、团队转发全部零改动。选项只写「一句能直接当答复读的话」，理由和取舍留在 `question` 里。上限常量（`MAX_QUESTION_OPTIONS` / `MAX_QUESTION_OPTION_LEN` / `MAX_QUESTION_ITEMS`）单点在 `shared`，server 校验、MCP 工具描述、网页渲染共用；超限一律 400 **不静默截断**（悄悄砍掉一个候选，agent 以为它还在、用户压根没见过）。提问态的出现/消失走独立 SSE 事件 `task.question`（`task.status` 只带状态字段，不捎 question），否则答完卡片还杵在那像是没答上。理由：选项是给答复者省打字的快捷方式，不该在协议层多开一条「选项答复」的路。
- **一次问多个问题走 `tasks.question_items`**（json `{question, options?}[]`，上限 4 题）：顶层 `question` 退化成**共同背景/引言**，顶层 `options` 只服务旧式单问题——两者同传一律 400（否则引言会被当成一道可回答的题）。答复仍是**一条文本**走同一个 `/answer`：按 `【i】问题 / 答：…` 编号对应，**未答的必须显式写「(未答)」，不许静默省略**——agent 得能区分「用户跳过了」和「用户没看见」，省略会让它以为那题不存在、然后自己脑补一个答案。不做 `multiSelect`、不做选项的 `description`/`header`：前者已被「填入输入框 + 换行追加」天然覆盖，再加一套是机制打架；后者会鼓励把说明塞进选项，跟上一条拧着来。执行者提问转发给调度者时（`server/src/team/inbox.ts`）要把多问题连同候选完整展开，脏 JSON 解析失败退回纯问题、不拖垮通知。
- Codex 单任务每一轮必须保留失败证据链：`data/runs/<taskId>/` 下按 session+turn 写原始 `codex exec --json` 事件、stderr 和结构化 diagnostics；`turn.failed`、signal、静默非零退出等诊断同时写进会话 Markdown，保证实时页面与刷新后都能看到。诊断写入是 best-effort，不得反过来改变 agent 原本的退出结果。
- **所有 AI 调用统一走本地 CLI 执行器**，不存在第二条「直连 HTTP」的路。派任务、续跑与辩论都经 `resolveExecutor(type)` / `resolveExecutorById(id)` 起一个真 CLI 进程。供应商（`llm_providers`）**只是执行器的一个属性**（`agents.provider_id`）：挂上就在起 CLI 前注入 base_url + key（claude 走 `ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN` 环境变量；codex 走 `-c model_providers.harness_relay.*` 且 `env_key` 指向环境变量名），于是 `claude@官方` 和 `claude@公司自建` 是两个并列的执行器，都能执行任务。`server/src/llm.ts` 只剩 `listModels`（配供应商时拉模型列表）。理由：两套 AI 调用体系（CLI vs 直连）意味着两套配置 UI、两种能力上限（直连没工具、干不了活），合并成一条后「选谁干活」在任何场景下都是同一个下拉。
  - 推论一：**key 绝不进命令行**。`commandLine` 会存进 `sessions.command_line` 并在 UI 展示，所以 codex 用 `env_key` 而非 `bearer_token`，真 key 只走进程 env；另有 `redactSecrets()`（`server/src/executors/spawn.ts`）兜底脱敏。展示用的恢复命令带 env 前缀但 token 是占位符，存在 `sessions.relay_env`（`toSession` 恒重算 resumeCommand，不能指望 executor 当初写进 DB 的那份）。
  - 推论二：`llm_providers.base_url` 存**根地址**（不含 `/v1`），但读取侧一律过 `relayRoot()` / `relayApi()`（`server/src/llm.ts`）归一，不要自己拼字符串——历史数据和手滑都可能已经带了 `/v1`，硬拼会打到 `/v1/v1`。`ANTHROPIC_BASE_URL` 用 `relayRoot`（SDK 自己补 `/v1`），codex 的 `model_providers.*.base_url` 与 `listModels` 探测用 `relayApi`（已是 `/v2` 之类就原样保留）。
  - 推论三：挂了供应商的执行器，「模型」下拉列的是**该供应商 `/v1/models` 拉到的全名**（`claude-opus-4-5-…`），不是 CLI 别名（`opus`）——供应商认的是自己那套 id，别名多半不存在。所以 UI 上「供应商」chip 排在「模型」前面：先定上游，才知道有哪些模型可选。
- 任务执行器从 type 升级为 profile：`tasks.executor_id` / `TeamConfig.leadExecutorId` / `TeamConfig.workerExecutorId` 指向 `agents.id`，解析统一走 `resolveExecutorFor({ executorId, type })`（`server/src/executors/index.ts`）。规则是 **executorId 非空且查得到就用具体 profile；为空或已删除则按 agentType/team lead/worker 的 type 取当前默认执行器降级**。`agentType` 仍保留，用于展示、筛选和降级；删除执行器时把直接引用它的 `tasks.executor_id` 置空，team JSON 里的旧 id 也必须靠 `resolveExecutorFor` 平滑降级。任务返回给前端时带只读 `executorLabel`，team 对象里带 `leadExecutorLabel` / `workerExecutorLabel`，都来自当前 `agents.name`。新增任何“选谁干活”的表面都必须同时使用前端 `ExecutorPicker`、持久化 `executorId` 字段和后端 `resolveExecutorFor`；自查时 grep 写死的 agent 类型列表，辩论链路曾因此漏过一次。
- 任务级 `model` / `reasoningEffort` 非空时覆盖执行器 profile，null 时继续跟随 profile；覆盖解析的单点是 `resolveExecutorFor`，所有为任务启动 CLI 的 fresh/resume/辩论/团队路径都只把任务或 TeamConfig 上的值传进这里，不在各 executor 或调用点另造优先级。`speed` 仍只属于执行器 profile，不做任务级覆盖。
- **全局设置统一存进 `app_settings` 通用 KV 表**，读取时把持久化值覆盖到 shared 的 `DEFAULT_APP_SETTINGS` 上；`worktreeDefault` 出厂为 `true`。普通任务、团队调度台、辩论衍生任务以及 `batch_create_tasks` / `create_task_chain` 在调用方未传 `useWorktree` 时都跟随该默认，显式 true/false 优先，但项目不是 git repo 时一律落 false。团队 dispatch 出来的 worker 是例外：它的 `useWorktree ?? false` 表示“是否在团队共享目录之上再开一层隔离”，刻意不跟随全局默认。本条取代前文“团队 worktree 默认仍关闭”的旧口径。
