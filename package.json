{
  "name": "harness",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "shared",
    "server",
    "web-next",
    "mcp"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:all": "concurrently -n server,web -c blue,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "PORT=4317 npm -w server run dev",
    "dev:web": "npm -w web-next run dev",
    "build": "npm -w shared run build && npm -w web-next run build && npm -w server run build && npm -w mcp run build",
    "build:mobile": "cd mobile && npx expo export -p web",
    "restart": "bash scripts/restart.sh",
    "start": "npm -w server run start",
    "mcp": "npm -w mcp run start",
    "db:push": "npm -w server run db:push",
    "test:diagnostics": "npm -w server run test:diagnostics"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.2"
  },
  "allowScripts": {
    "esbuild@0.18.20": true,
    "esbuild@0.19.12": true,
    "esbuild@0.28.1": true,
    "esbuild@0.25.12": true
  }
}
