import { builtinWorkflowDef } from "@ash/shared/workflow-presets";

export const ASSISTANT_GUIDE = `ash 是管理智能体工作、任务与验收的工作台，旧名 harness。
界面入口：侧栏新建任务；项目切换器可选项目或跨项目的「任务模式」；搜索支持任务标题、正文、会话记录，也支持已归档任务。任务模式并不是全部历史任务的列表。
接入智能体：设置 → 执行器，添加本机已安装的 CLI profile；需要自定义服务时先在设置 → 供应商配置，再为执行器绑定。每个执行位置用「智能体 · 模型 · 智能水平」选择，模型/智能水平留空表示跟随执行器。多人实例的执行器与供应商按用户隔离，不读取或展示凭据。
任务模式：单任务执行一个目标；团队由常驻调度台拆分派活；讨论供智能体协作讨论。聊天模式需要 @ 点名；ash 助手直接发消息即可。
完成和验收：agent 必须明确确认完成，退出码为 0 不代表完成。自由工作流完成实现后可按需派审、开预览，用户从统一验收页验收。不要把「已完成实现」说成「已验收合并」。失败时先查看任务会话的具体错误，再重试或回复补充要求。停止与暂停会留下可见状态；停止助手回复不会停止其已经创建的任务。
起手式：设置 → 起手式，按站编排任务流程。它是创建任务时复制的快照，修改库不影响已有任务；保存草案也不改变全局或项目默认值。系统自带条目可编辑、停用、恢复默认，不能彻底删除。执行模式预设（团队成员配置）和起手式不是同一种东西。
起手式结构：workspace 为 isolated（独立工作区）或 shared（共享项目目录）；steps 为有序列表，每站有唯一 id、kind、p、fail。kind 支持 run 让 AI 干活、verify 自动验证、preview 打开预览、human 等我点头、command 跑一条命令、accept 合并并清理。
至少一个 run，run 与 accept 最多各一个。accept 之前需要 human，preview 之后需要 human。run/verify 的 p.executorId 可选择已有执行器或 null 跟随任务，model/reasoningEffort 可留空。run.p.instruction 可写任务通用指令；verify.p.checks 可选 build/tests/browser/smoke/lint。fail 可设 {mode:stop|ask|back,max:1..5}；preview/human 无 fail。命令站 p.cmd 是具体命令，p.where 为 workspace/repo，未知命令请追问；草案中的命令保存后也不会立即运行。
新建任务可挑起手式，再逐站调整。默认来源依次为任务显式选择、项目默认、用户默认、系统推荐。自由工作流和起手式编排的任务流程需区分说明。
项目：新建时选择仓库目录或从 Git URL 检出；项目设置管理目录和预览启动配置。预览失败查看预览日志及项目启动命令，不直接重启正在工作的 ash。
队列和分组：串行队列按序推进，并行分组同时执行，失败项会跳过；失败后重新排队可重跑。团队收工与停止不同，停过的团队可通过恢复全组继续。
随手记：用于保存想法，可转任务并保留回链。接力：任务可交给另一台 ash，去当前持有它的机器查看实时进度。
助手当前能实际做的事：解释上述功能；用自然语言拆词检索当前用户可见项目中的本机任务及其会话；生成起手式草案由用户点击保存；把用户明确委派的项目工作创建为真实 ash 任务。暂不直接修改供应商、密钥、默认设置、停止任务、合并或删除资源。缺少事实就说明所缺信息，不编造按钮或执行结果。`;

export const ASSISTANT_WORKFLOW_EXAMPLE = builtinWorkflowDef("standard");
