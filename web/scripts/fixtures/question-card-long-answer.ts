export const longQuestion = `跨站那个问题，代码已经修好了，只是还在任务分支上等验收。你重启的那台 ash 跑的是 main 的构建，所以行为没变。验收后重建重启就好。

现在要你定的还是上一轮那道题：预览里的登录墙怎么办。三个选项（正文里有完整的操作说明）：

A = 被预览的仓库就是这台 ash 自己时，自动把当前会话放进这趟预览的服务端容器，打开即登录态。页面拿不到 cookie 本身，但它能用你的身份访问 ash 的 API。别的项目的预览一律不受影响。

B = 隔离一点都不动，只在预览里显眼提示「别在这儿粘 key」。

零风险，但「只启动前端」这一档基本废掉。

C = 同 A，但做成起预览时的开关，默认关。我推荐这个：默认安全，要看真实数据时一键开启。

另外还有一个同源的遗留问题要不要一并修：sandboxOriginated 要求 Origin:null 且 Sec-Fetch-Site:cross-site 才转发应用自己的 Authorization，而明文 http 下第二个条件永远不成立。用 Bearer token 的被预览应用，在这种部署下登录同样会失效。改法只有一处，但它是前两轮审查专门收紧过的安全判断，我不擅自放开。

这两项会影响预览时的登录体验。请确认默认的处理方案，以及是否将同源的遗留问题一起修复。选择之后我会按你的决定继续处理，并保留必要的回归检查。`;

export const longAnswer = `A
顺便把你说的那个遗留问题一并修了：sandboxOriginated 在明文 http 下永远不成立，用 Bearer 的被预览应用同样登录不了。
再就是我想多问一句，只有在 ash 预览 ash 才这样吗？还是在 ash 上预览别的项目就没这些麻烦事了？`;

export const longLegacyReply = `  【答复】你之前的提问:「${longQuestion}」\n\n${longAnswer}`;
