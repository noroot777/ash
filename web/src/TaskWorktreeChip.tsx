export function TaskWorktreeChip() {
  return (
    <span
      className="inline-flex shrink-0 items-center rounded border border-line bg-raised px-1.5 py-0.5 font-mono text-[10px] font-medium text-muted"
      title="此任务在独立 git worktree 中运行"
    >
      worktree
    </span>
  );
}
